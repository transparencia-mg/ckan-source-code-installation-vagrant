Ckan Source code Installation using Vagrant
===

- Clone the repository and install.

```shell
git clone git@github.com:transparencia-mg/ckan-source-code-installation-vagrant.git
cd ckan-source-code-installation-vagrant
vagrant up
```